@model IEnumerable<M_ventas_y_cc.Models.DETALLES_NOTAS_DE_CREDITO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.numDetalleNC)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.subtotal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.numDetalleNC)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.subtotal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DETALLES_NOTAS_DE_CREDITOId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DETALLES_NOTAS_DE_CREDITOId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DETALLES_NOTAS_DE_CREDITOId })
        </td>
    </tr>
}

</table>
